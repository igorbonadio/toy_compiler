# Default IID definition

X = Alphabet(a, t, g, c) # X is the DNA's alphabet

# dna is an IID model
dna = IID(X):
  distribution = Prob( # a distribution is definded by a list of probabilities
    a = 0.1
    t = 0.3
    g = 0.4
    c = 0.2
  )
